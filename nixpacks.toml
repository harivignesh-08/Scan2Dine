# Nixpacks Configuration for Unified Spring Boot + React Deployment on Railway
providers = ["node", "java"]

[phases.build]
cmds = [
  "cd frontend && npm install && npm run build",
  "cd backend && mvn package -DskipTests"
]

[start]
cmd = "java -jar backend/target/api-1.0.0-SNAPSHOT.jar"
